#include <stdio.h>

/**
 *i main - program that prints the alphabet in lowercase and upper case
 *
 * Return: Always 0
 */

int main(void)
{
        char l;

	for (i = 'a' ; i<= 'z' ; i++)
		putchar(l);
	for (i = 'A' ; i<= 'Z' ; i++)
		putchar(l);
	putchar('\n');

        return (0);
} 
